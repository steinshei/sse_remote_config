name: remote_config_lib
description: Self-hosted remote config client SDK skeleton for Flutter (REST, MMKV cache, stable A/B buckets; SSE stub).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  mmkv: ^1.3.0
  connectivity_plus: ^6.1.4
  crypto: ^3.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
